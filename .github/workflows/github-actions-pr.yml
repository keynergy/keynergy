name: GitHub Actions PR
on: [push]
jobs:
  build:
  
    runs-on: ubuntu-latest
    
    steps:
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
